model: HuggingFaceTB/SmolLM2-135M-Instruct
tokenizer: HuggingFaceTB/SmolLM2-135M-Instruct

dataset:
  type: local
  path: data/processed/foundation_train.jsonl
  format: jsonl
  split: train

chat_template: |
  {% for message in messages %}
    {% if loop.first and messages[0]['role'] != 'system' %}
      {{ '<|im_start|>system\nYou are a medical coding expert.<|im_end|>\n' }}
    {% endif %}
    {{ '<|im_start|>' + message['role'] + '\n' + message['content'] + '<|im_end|>\n' }}
  {% endfor %}
  {% if add_generation_prompt %}
    {{ '<|im_start|>assistant\n' }}
  {% endif %}

tokenizer_additional_special_tokens:
  - "<think>"
  - "</think>"

trainer:
  per_device_train_batch_size: 8
  gradient_accumulation_steps: 4
  num_train_epochs: 3
  max_steps: -1
  learning_rate: 3.0e-4
  weight_decay: 0.01
  warmup_ratio: 0.1
  max_grad_norm: 1.0
  lr_scheduler_type: cosine
  
  save_strategy: "steps"
  save_steps: 500
  save_total_limit: 3
  
  logging_steps: 10
  eval_strategy: "steps"
  eval_steps: 250
  
  output_dir: "outputs/foundation_sft"
  run_name: "thce-foundation-sft"
  
  bf16: true
  gradient_checkpointing: true
  max_seq_length: 2048
  
  report_to: "wandb"

